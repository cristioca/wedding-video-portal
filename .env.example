DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wedding_portal?schema=public"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-long-random-string"

# By default, the seed script will create an admin user with these credentials
ADMIN_EMAIL=admin@admin.com
ADMIN_PASSWORD=password123

# Email configuration (for sending client credentials, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASS=your_smtp_password
SMTP_FROM_EMAIL=noreply@example.com
